export class MyBadge {
  constructor(title, details, appearance, detailsArray) {
    this.title = title;
    this.details = details;
    this.appearance = appearance;
    this.detailsArray = detailsArray;
  }
}
